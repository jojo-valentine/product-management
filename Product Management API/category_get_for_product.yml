info:
  name: category_get_for_product
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}/api/category/for-product"
  headers:
    - name: Content-Type
      value: application/json
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: name
      value: etan
    - name: description
      value: adsadsasad
    - name: status
      value:
        type: boolean
        data: "true"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
